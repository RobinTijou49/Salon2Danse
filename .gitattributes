# Normalise toutes les fins de ligne en LF dans le dépôt.
# Évite le classique "bad interpreter" sur les .sh une fois déployés sur Linux.
* text=auto eol=lf

# Fichiers binaires : jamais de conversion
*.png binary
*.jpg binary
*.ico binary
